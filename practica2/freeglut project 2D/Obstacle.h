#pragma once

#include "PV2D.h"

class Obstacle{
public:
	//virtual bool intersectionLine2Triangle (PV2D* p, PV2D* v, double& tIn, PV2D*& normalIn);
	//virtual bool collisionDetection(double& tIn, PV2D*& normalIn);
};